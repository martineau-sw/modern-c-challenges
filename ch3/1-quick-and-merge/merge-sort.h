#ifndef MERGE_SORT_HEADER
#define MERGE_SORT_HEADER
#include <stddef.h>

void merge_sort_doubles(size_t start, size_t end, double* A);
void merge_sort_strings(size_t start, size_t end, char** A);
#endif
